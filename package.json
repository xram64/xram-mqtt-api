{
  "name": "xram-mqtt-api",
  "version": "0.1.0",
  "description": "A REST API for the Mosquitto MQTT broker.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "xram",
  "license": "ISC",
  "private": true,
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
